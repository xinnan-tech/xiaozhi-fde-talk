[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
addopts = -q -m "not e2e"
markers =
    integration: 依赖运行中的后端服务
    contracts: 外部端口契约测试
    e2e: 端到端混沌测试（需真实后端 + 真实 ASR/LLM，pytest -m e2e 运行）
filterwarnings =
    ignore::RuntimeWarning
