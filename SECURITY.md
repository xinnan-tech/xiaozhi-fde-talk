# 安全策略

## 报告漏洞

如果你发现了潜在的安全问题，**请不要** 走公开 issue 渠道。请把详情（复现步骤、
影响范围、版本号、修复建议如有）发到：

**security@xinnan-tech.com**

我们会在 3 个工作日内确认收悉，并在 14 个工作日内给出处理计划（修复 / 评估 /
延期理由）。对负责任披露者，我们会在修复发布后致谢（如你同意）。

## 支持的版本

仅 `main` 分支接受安全修复。已发布的旧版本（如有 tag）不再维护。

| 分支 / 版本 | 支持状态 |
| --- | --- |
| `main` | 受支持 |
| 旧版 release tag | 不再维护，请升级 |

## 响应 SLA

- 初次确认：3 个工作日内。
- 影响评级（CVSS 思路）：7 个工作日内。
- 关键 / 高危漏洞修复：30 天内发布。
- 中危：90 天内修复或给出缓解建议。
- 低危：随下次常规版本发布。

## 已知披露过的领域

下列模块曾在安全审计中强化过，欢迎针对它们做进一步审查：

- JWT 认证（`localStorage` 落盘风险，已有 httpOnly 迁移计划）
- WebSocket 子协议鉴权（token 仅走 `bearer.<jwt>`）
- Markdown 报告渲染（已禁 `html: true`）
- 注册 / 改密端点限流（IP + 用户名双维度）
- CORS 白名单（prod 必填）

## 不在范围内

- 依赖链漏洞：欢迎报告，我们会在 PR 中升级或替换，但不会为此单独发安全公告，
  请跟随 GitHub Dependabot / Renovate 的提示。
- 自托管实例的配置失误（如 CORS 留空、admin 默认密码）：属于运维责任，请按
  `backend/.env.example` 与 README 的部署章节自检。