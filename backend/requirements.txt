# 后端依赖锁文件（pip 全锁 exact 版本）
# 这是一个 pinned lockfile：升级须有充分的兼容性 review + 更新全套依赖 + 本地全量回归。
# 解开 == 之前先看 git blame 与 Wave 3 引用的依赖版本基线。
#
# bcrypt: 4.1 起对超过 72 字节的密码直接抛 ValueError（不再静默截断），避开
# 4.0.x 的截断导致不同等长末段 password 哈希一致的等效碰撞攻击面。password_policy
# 已强校验长度，实际风险很小，但仍升到 4.1+。

# Web 框架
fastapi==0.115.6
uvicorn[standard]==0.32.1
websockets==13.1
httpx==0.28.1
python-dotenv==1.0.1
pydantic==2.10.4
pydantic-settings==2.7.0

# 数据库（MVP: SQLite + SQLAlchemy async 后续切换到 MySQL）
SQLAlchemy[asyncio]==2.0.36
aiosqlite==0.20.0

# 数据库迁移（prod schema 版本管理；dev 默认仍走 create_all）
alembic==1.14.0

# 鉴权
PyJWT==2.10.1
# bcrypt >= 4.1: 4.0.x silent-truncates passwords > 72 bytes (UTF-8).
# Earlier caps in password_policy.py mitigate but version bump removes
# the silent failure mode entirely.
bcrypt==4.2.1

# 日志
structlog==24.4.0

# 报告导出
bleach==6.2.0
markdown==3.7
python-docx==1.1.2

# 音频处理（WebM 解码）
onnxruntime==1.19.2
llvmlite==0.43.0
numpy==1.26.4
av==12.3.0
